# default configuration
default: |
  analyzers:
    - inherit: qa4ds.pipeline.simple-dummy-pipeline
  mergers:
    - inherit: base.noop
# configuration for
property-flood-composite: |
  analyzers:
    - inherit: qa4ds.pipeline.simple-dummy-pipeline
    - inherit: qa4ds.pipeline.full-dummy-pipeline
  mergers:
    - inherit: base.noop
