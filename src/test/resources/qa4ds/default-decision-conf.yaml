  # default configuration
  default: |
    analyzers:
      - inherit: qa4ds.pipeline.simple-test-pipeline
    mergers:
      - inherit: base.noop
    rankers:
      - inherit: base.noop
    pruners:
      - inherit: base.noop
